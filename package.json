{
  "name": "banner-practise",
  "private": true,
  "description": "SPA: рекламные тексты и баннеры через GigaChat, дублирование результатов в Telegram (Express + React + Vite).",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
