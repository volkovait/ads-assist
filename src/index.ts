/**
 * Точка входа Express для Vercel (см. https://vercel.com/docs/frameworks/backend/express ).
 * Локально: `npm run start -w server` — server/src/index.ts
 */
export { default } from '../server/src/index.ts';
